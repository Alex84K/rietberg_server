services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rietberg_server
    ports:
      - "5008:5008"
    env_file:
      - .env
    environment:
      - PORT=5008
    restart: always